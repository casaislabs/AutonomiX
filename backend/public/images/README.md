Place static agent images here.

Suggested convention:
- `agent<ID>.png` or `agent<ID>.svg`, e.g., `agent1.svg`.
- Public paths are served under `/images`, e.g., `/images/agent1.svg`.

Make sure the files referenced by metadata exist.